import { Button } from "@/components/ui/button";
import useAuth from '@/hooks/useAuth';
import { LogOut, User } from "lucide-react";
import { Link } from "react-router-dom";

// Removed UserMenu component as it is no longer needed.
